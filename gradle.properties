org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Minecraft / Fabric target
# NOTE: retargeted from 26.1.2 (no Yarn yet) to 1.21.11 (latest stable with Yarn).
# To move to 26.x later, switch mappings to loom.officialMojangMappings() and
# rewrite imports (VillagerEntity -> Villager, TradeOffer -> MerchantOffer, etc.).
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.6
loader_version=0.19.2

# Fabric API
fabric_version=0.141.4+1.21.11

# Mod identity
mod_id=tradeoptimizer
mod_version=0.1.0
maven_group=com.tom
archives_base_name=tradeoptimizer

# Java toolchain
java_version=25
