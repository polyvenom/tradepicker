modLoader = "javafml"
loaderVersion = "[1,)"
license = "CC0-1.0"

[[mods]]
modId = "tradeoptimizer"
version = "0.0.0"
displayName = "Trade Picker"
description = "Pick villager trades from a menu instead of cycling."

# Mixins (annotation processor wired automatically by ModDevGradle). NeoForge runs on
# Mojmaps so these need no refmap. VillagerInvoker = the special-prices @Invoker; the client
# config injects the Reset button into the merchant screen.
[[mixins]]
config = "tradeoptimizer.mixins.json"

[[mixins]]
config = "tradeoptimizer.client.mixins.json"

[[dependencies.tradeoptimizer]]
modId = "neoforge"
type = "required"
versionRange = "[26.1.2,)"
ordering = "NONE"
side = "BOTH"

[[dependencies.tradeoptimizer]]
modId = "minecraft"
type = "required"
versionRange = "[26.1.2,)"
ordering = "NONE"
side = "BOTH"
